all:
	@ cp -r debian build
	@ cp -r ../../http build/debian
	@ cp -r ../../libremote build/debian
	@ cp -r ../../player build/debian
	@ cp -r ../../remote build/debian
	@ cp -r build/debian build/deb
	@ cp -r build/debian build/bionic
	@ cp -r build/debian build/focal
	@ rm -r build/debian
